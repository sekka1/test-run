---

build:
  box:
    id: ubuntu:14.04.4
    bin: /bin/bash -x

  steps:
    - script:
        name: run1
        code: |
          ls -l
          echo "xxxxxxxxx"
          pwd
          echo "xxxxxxxxx"
          ls -l /pipeline
          echo "xxxxxxxxx"
          cat /pipeline/source/run1.sh
          # echo "xxxxxxxxx"
          ls -l /pipeline/source
          echo "xxxxxxxxx"
          #cat /pipeline/source/run1.sh

    - script:
        name: two
        cwd: /pipeline/source
        code: |
          ./run1.sh
