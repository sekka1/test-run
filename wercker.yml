---

build:
  box:
    id: ubuntu:14.04.4

  steps:

    - script:
        name: hit mongo
        code: |
          apt-get update
          apt-get install -y curl

          # echo "curl https://kubernetes"
          # curl -v https://kubernetes --insecure

          # echo "mongo staging: curl -v 10.15.13.252:27017"
          # curl -v 10.15.13.252:27017


          echo "mongo prod: curl mongodb-01-app.wercker.com:27017"
          curl -v mongodb-01-app.wercker.com:27017


    # - script:
    #     name: run1
    #     code: |
    #       ls -l
    #       echo "xxxxxxxxx"
    #       pwd
    #       echo "xxxxxxxxx"
    #       ls -l /pipeline
    #       # echo "xxxxxxxxx"
    #       ls -l /pipeline/source
    #       echo "xxxxxxxxx"
    #       cat /pipeline/source/runs1.sh
    #
    #
    # - script:
    #     name: two
    #     cwd: /pipeline/source
    #     code: |
    #       /pipeline/source/runs1.sh
