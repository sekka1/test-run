---

build:
  box:
    id: ubuntu:14.04.4
    bin: /bin/bash -x

  steps:
    - script:
        name: run1
        code: |
          ls -l
          pwd
          chmod 777 run1.sh
          cat run1.sh
          ls /pipeline/source
          #cat /pipeline/source/run1.sh
